import React, { Component } from "react";
import { SearchView } from "../views";
import { SearchTrains } from "../queries";

class SearchContainer extends Component {
  render() {
    return (
      <>
        <SearchView />
      </>
    );
  }
}

export default SearchContainer;
